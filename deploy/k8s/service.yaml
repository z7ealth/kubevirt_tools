apiVersion: v1
kind: Service
metadata:
  name: kubevirt-tools-service
  namespace: kubevirt-tools
spec:
  selector:
    app: kubevirt-tools-app
  ports:
    - name: http
      port: 80
      targetPort: 4000
  type: ClusterIP
